import { PrecioCantidadPipe } from './precio-cantidad.pipe';

describe('PrecioCantidadPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecioCantidadPipe();
    expect(pipe).toBeTruthy();
  });
});
