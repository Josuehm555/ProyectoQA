import { PrecioTotalPipe } from './precio-total.pipe';

describe('PrecioTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecioTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
