import { PrecioImpuestoPipe } from './precio-impuesto.pipe';

describe('PrecioImpuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecioImpuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
